import { GuidedChat } from '@/features/conversation/guided-chat';

export const metadata = {
  title: 'Planning your trip — Wayfare',
};

export default function ChatPage() {
  return <GuidedChat />;
}
